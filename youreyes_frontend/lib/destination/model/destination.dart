import 'package:youreyes_frontend/userLocation/model/model.dart';

class Destination{
  final Map<String,double> destination;
  final UserLocation userLocation;
  Destination({required this.destination, required this.userLocation});
}